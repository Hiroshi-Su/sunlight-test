# 日差しと連携するジェネラティブ映像 — 案件整理メモ

設置場所：ソラシティ カンファレンスセンター前ロビー（東京・御茶ノ水）
作成日：2026-09-24

---

## 1. 案件概要

日差しの入るロビーで、2台のプロジェクターによる横長スクリーンに、その地域の空や気象をテーマにした穏やかなジェネラティブ映像を、オフラインかつ低負荷のPCで流す案件。

### 前提条件

| 項目 | 内容 |
|---|---|
| テーマ | 空、その地域にまつわる映像 |
| 表示環境 | 日差しあり。黒の階調は出にくく、白が多いと眩しい |
| PC | 4090 / 5090 クラスをフルに回せる電力はない |
| 表現 | 写真的な鮮明さ、細かい粒子などの高精細表現は不向き |
| ネット | 接続できない可能性あり → 完全オフラインで動作させる |
| データ | 気象庁の静的データ（CSV）を活用 |
| 出力 | Full HD プロジェクター × 2台（横並び） |
| キャンバス | 基本 3840 × 1080（32:9）。重なり幅により実際に見える横幅は狭くなる |
| ブレンディング | 別会社が担当 |

### 表現の方向性

- 純黒と純白を避け、中間〜やや明るめの輝度で、ある程度彩度のある**色面**で見せる。
- 大きなグラデーション、ゆっくり動く色面、低周波ノイズ、大気のような柔らかい揺らぎなど、解像感に頼らない表現。
- 大きなグラデーションは 8bit 出力で縞（バンディング）が出るため、シェーダー側で**ディザ**を入れる前提で組む。

---

## 2. 設置環境（現地写真から）

- 左側が大きなガラス窓で、時間帯によって横から日差しが入る。
- 投影面はガラスの間仕切り。複数のフィルムや紙を貼って比較検証中。
- ガラスには窓の外の景色やロビーが**映り込み**、投影像と重なって見える。
- ガラスパネルの**縦の継ぎ目**が一定間隔で映像を横切る。構図に取り込める要素。
- 白いキューブの映像は形が読めていた一方、カラフルな映像は外光で色が浅くなっていた。→ 明度差・面構造で見せる方が強い。
- 手前テーブルからのフロント投影。人が横切ると影が出る点は要確認。

---

## 3. コンセプト：差し込む日差しと連携する

実際に差し込む光と、映像内の「仮想の光」を同期させ、画面の外から光が続いているように見せる。

| 層 | 手法 | 位置づけ |
|---|---|---|
| 1 | 太陽位置の計算（センサー不要） | **ベース** |
| 2 | 照度センサーによるリアルタイム補正 | 設置可能なら追加 |
| 3 | 気象庁データによる季節の文脈 | コンセプトの厚み |

表現アイデア：

- **光の続き**：窓枠やガラスの継ぎ目の影が伸びる方向に、映像内の光の帯・面がつながっていく。
- **光の時計**：一日を通して光の角度と色が実際の太陽に合わせて移ろう。
- **負ける時間も演出に**：光が画面に直接当たる時間帯は、「光を受け止める」白っぽい面構成に切り替える。

---

## 4. 太陽位置の計算

### 4.1 自転・公転でずれないのか

ずれない。入力が「緯度経度と日時」だけでも、式の内部で自転と公転の影響を計算しているため。

- **自転（1日の動き）**：時刻から、太陽が真南からどれだけ回ったか（**時角**）を求める。1時間で15°。
- **公転（1年の動き）**：日付から地球の軌道上の位置を求め、太陽の高さの季節変化（**赤緯**、夏至で約+23.4°、冬至で約−23.4°）を算出する。
- **軌道の楕円と地軸の傾きの補正**：太陽が真南に来る時刻は季節により最大±16分ほど前後する。この補正（**均時差**）も式に含まれる。
- 長期的な変化（歳差、章動など）も、採用するアルゴリズムに応じて織り込まれている。

### 4.2 計算の流れ

```
日時(UTC) ──▶ ユリウス日 JD ──▶ ユリウス世紀 T = (JD − 2451545.0) / 36525
                                       │
          ┌────────────────────────────┴───────────────────────┐
          ▼                                                    ▼
 太陽の平均黄経 L0・平均近点角 M・離心率 e             黄道傾斜角 ε
          │                                                    │
          ▼                                                    │
 中心差 C → 真黄経 → 視黄経 λ                                   │
          │                                                    │
          └──────────────┬─────────────────────────────────────┘
                         ▼
              赤緯 δ = asin( sin ε · sin λ )
              均時差 EoT
                         │
                         ▼
   真太陽時 TST = UTC(分) + EoT + 4 × 経度      （分）
   時角 H = TST / 4 − 180                       （度）
                         │
                         ▼
   cos(天頂角 Z) = sin φ · sin δ + cos φ · cos δ · cos H     （φ = 緯度）
   高度 = 90° − Z
   方位角 = atan2( sin H , cos H · sin φ − tan δ · cos φ ) + 180°
```

### 4.3 主要な式（NOAA / Meeus 簡略版）

| 量 | 式（角度は度） |
|---|---|
| 平均黄経 L0 | 280.46646 + T(36000.76983 + 0.0003032T) |
| 平均近点角 M | 357.52911 + T(35999.05029 − 0.0001537T) |
| 離心率 e | 0.016708634 − T(0.000042037 + 0.0000001267T) |
| 中心差 C | sin M(1.914602 − T(0.004817 + 0.000014T)) + sin 2M(0.019993 − 0.000101T) + 0.000289 sin 3M |
| 視黄経 λ | L0 + C − 0.00569 − 0.00478 sin Ω（Ω = 125.04 − 1934.136T） |
| 黄道傾斜角 ε | 23°26′21.448″ − 46.815″T − … + 0.00256 cos Ω |
| 赤緯 δ | asin(sin ε · sin λ) |
| 均時差 EoT（分） | 4 · deg( y sin 2L0 − 2e sin M + 4ey sin M cos 2L0 − ½y² sin 4L0 − 1.25e² sin 2M )、y = tan²(ε/2) |

### 4.4 実装コード

以下のコードは同じ式を JavaScript（three.js 用）と Python（TouchDesigner 用）で実装したもの。
**検証**：東京（35.6993N, 139.7655E）の夏至正午・冬至正午・2026/9/24 の 9時・15時で、Python ライブラリ `pysolar` の結果と比較し、方位角・高度とも差は約0.03°以内（差の大部分は `pysolar` 側の大気差補正によるもの）。

#### JavaScript（three.js）

```javascript
// 戻り値: azimuth(北=0°, 東=90°), altitude(水平=0°), declination, equationOfTime(分)
function sunPosition(date, lat, lon) {
  const rad = Math.PI / 180, deg = 180 / Math.PI;
  const jd = date.getTime() / 86400000 + 2440587.5;
  const T = (jd - 2451545.0) / 36525.0;
  const L0 = ((280.46646 + T * (36000.76983 + 0.0003032 * T)) % 360 + 360) % 360;
  const M = 357.52911 + T * (35999.05029 - 0.0001537 * T);
  const e = 0.016708634 - T * (0.000042037 + 0.0000001267 * T);
  const Mr = M * rad;
  const C = Math.sin(Mr) * (1.914602 - T * (0.004817 + 0.000014 * T))
          + Math.sin(2 * Mr) * (0.019993 - 0.000101 * T)
          + Math.sin(3 * Mr) * 0.000289;
  const omega = 125.04 - 1934.136 * T;
  const lambda = L0 + C - 0.00569 - 0.00478 * Math.sin(omega * rad);
  const eps0 = 23 + (26 + (21.448 - T * (46.815 + T * (0.00059 - T * 0.001813))) / 60) / 60;
  const eps = eps0 + 0.00256 * Math.cos(omega * rad);
  const decl = Math.asin(Math.sin(eps * rad) * Math.sin(lambda * rad)) * deg;
  const y = Math.tan(eps * rad / 2) ** 2;
  const L0r = L0 * rad;
  const eot = 4 * deg * (y * Math.sin(2 * L0r) - 2 * e * Math.sin(Mr)
            + 4 * e * y * Math.sin(Mr) * Math.cos(2 * L0r)
            - 0.5 * y * y * Math.sin(4 * L0r) - 1.25 * e * e * Math.sin(2 * Mr));
  const minutesUTC = date.getUTCHours() * 60 + date.getUTCMinutes() + date.getUTCSeconds() / 60;
  const tst = ((minutesUTC + eot + 4 * lon) % 1440 + 1440) % 1440;
  const ha = (tst / 4 - 180) * rad;
  const latr = lat * rad, dr = decl * rad;
  const cosZ = Math.sin(latr) * Math.sin(dr) + Math.cos(latr) * Math.cos(dr) * Math.cos(ha);
  const altitude = 90 - Math.acos(Math.min(1, Math.max(-1, cosZ))) * deg;
  const azimuth = (Math.atan2(Math.sin(ha),
      Math.cos(ha) * Math.sin(latr) - Math.tan(dr) * Math.cos(latr)) * deg + 180 + 360) % 360;
  return { azimuth, altitude, declination: decl, equationOfTime: eot };
}

// 使用例（ソラシティ付近の概算座標。正確な値は地図で要確認）
const sun = sunPosition(new Date(), 35.6993, 139.7655);
```

> ライブラリを使う場合は `SunCalc`（npm: `suncalc`）でも可。ファイルを同梱すればオフラインで動作する。

#### Python（TouchDesigner）

```python
import math
from datetime import datetime, timezone

def sun_position(dt_utc, lat, lon):
    """戻り値: (方位角[北=0°, 東=90°], 高度[°])。dt_utc はタイムゾーン付き datetime"""
    jd = dt_utc.timestamp() / 86400.0 + 2440587.5
    T = (jd - 2451545.0) / 36525.0
    L0 = (280.46646 + T*(36000.76983 + 0.0003032*T)) % 360
    M = 357.52911 + T*(35999.05029 - 0.0001537*T)
    e = 0.016708634 - T*(0.000042037 + 0.0000001267*T)
    Mr = math.radians(M)
    C = (math.sin(Mr)*(1.914602 - T*(0.004817 + 0.000014*T))
         + math.sin(2*Mr)*(0.019993 - 0.000101*T) + math.sin(3*Mr)*0.000289)
    omega = 125.04 - 1934.136*T
    lam = L0 + C - 0.00569 - 0.00478*math.sin(math.radians(omega))
    eps0 = 23 + (26 + (21.448 - T*(46.815 + T*(0.00059 - T*0.001813)))/60)/60
    eps = eps0 + 0.00256*math.cos(math.radians(omega))
    decl = math.degrees(math.asin(math.sin(math.radians(eps))*math.sin(math.radians(lam))))
    y = math.tan(math.radians(eps/2))**2
    L0r = math.radians(L0)
    eot = 4*math.degrees(y*math.sin(2*L0r) - 2*e*math.sin(Mr)
                         + 4*e*y*math.sin(Mr)*math.cos(2*L0r)
                         - 0.5*y*y*math.sin(4*L0r) - 1.25*e*e*math.sin(2*Mr))
    u = dt_utc.astimezone(timezone.utc)
    minutes = u.hour*60 + u.minute + u.second/60
    tst = (minutes + eot + 4*lon) % 1440
    ha = math.radians(tst/4 - 180)
    latr, dr = math.radians(lat), math.radians(decl)
    cosz = math.sin(latr)*math.sin(dr) + math.cos(latr)*math.cos(dr)*math.cos(ha)
    alt = 90 - math.degrees(math.acos(max(-1.0, min(1.0, cosz))))
    az = (math.degrees(math.atan2(math.sin(ha),
          math.cos(ha)*math.sin(latr) - math.tan(dr)*math.cos(latr))) + 180) % 360
    return az, alt

# TouchDesigner: Execute DAT の onFrameStart などで数秒おきに呼び、CHOP/Constant に書き出す
az, alt = sun_position(datetime.now(timezone.utc), 35.6993, 139.7655)
```

### 4.5 部屋の座標への変換

計算で得た方位角は**真北基準**。映像に反映するには、スクリーンの向きとの関係に変換する。

- `F`：鑑賞者がスクリーンを見る向きの方位角（真北基準）
- `R = F + 90°`：スクリーンの「右」方向の方位角

光の進む向きをスクリーン平面（右 = +x、上 = +y）に投影した2Dベクトル：

```
light.x = −cos(高度) · cos(方位角 − R)
light.y = −sin(高度)
```

- 太陽がスクリーンの左側にあるとき、光は右向き（+x）に進む。
- `light` を正規化して、映像内の光の帯の角度や影の伸びる向きに使う。
- 窓から光が入るかどうかは、「高度 > 0」「太陽が窓の外側の方向にある」「周囲のビルより高い」の3条件で判定する。最後の条件は計算で分からないため、現地観察で時間帯ごとの**遮蔽テーブル**を作っておく。

### 4.6 色温度のマッピング（演出上の目安）

厳密な物理値ではなく、演出のための目安。

| 太陽高度 | 色温度の目安 | 色の印象 |
|---|---|---|
| 0°以下 | 〜2000K | 残照、群青〜紫 |
| 0〜5° | 2500〜3500K | 琥珀、橙 |
| 5〜20° | 3500〜5000K | 暖かい白 |
| 30°以上 | 5500〜6000K | ニュートラル |

パレットを高度で補間する方式にすると、外光下での見え方に合わせて現地で色を追い込みやすい。

### 4.7 ずれの原因になりうるもの

| 要因 | 影響 | 対策 |
|---|---|---|
| PC時計のずれ | オフラインだと補正されない。1日数秒程度。太陽は1分で約0.25°動くだけなので実用上ほぼ問題なし | 設置時に時刻合わせ。タイムゾーン（JST, UTC+9）を確実に設定 |
| 方角の基準 | スマホのコンパスは磁北基準。東京では真北と約7°ずれる | 図面や地図アプリで真北基準の向きを確認 |
| 周囲の建物 | 計算上は太陽が出ていても光が入らない時間帯がある | 現地で数時間おきに写真を撮り、遮蔽テーブルを作る |
| 地平線付近の大気差 | 低高度で最大約0.5°ずれる | この立地ではビルに隠れる時間帯なので実質無視できる |

---

## 5. 参考文献・アルゴリズムの詳細

用途に対して精度は十分すぎるほどなので、**実装の簡単さ**で選べばよい。本メモのコードは 5.1〜5.2 の系統。

### 5.1 Jean Meeus『Astronomical Algorithms』

- Meeus, J. *Astronomical Algorithms*. Willmann-Bell, 1991（第2版 1998）。
- 天文計算の定番書。ユリウス日、太陽の黄経、黄道傾斜角、章動、均時差などを、段階的な手順として体系化している。
- 太陽位置については、簡略法（精度約0.01°）と、VSOP87理論に基づく高精度法の両方を解説。
- 本メモのコードは簡略法をベースにしている。

### 5.2 NOAA Solar Calculator

- アメリカ海洋大気庁（NOAA）の Global Monitoring Laboratory が公開している太陽位置計算ツールと、その計算式（スプレッドシート形式で配布）。
- Meeus の簡略法をもとにしており、1800〜2100年の範囲で実用上十分な精度（高度・方位で約0.01°程度）とされる。
- 式が短く、シェーダーやスクリプトに移植しやすいため、映像表現用途には最も扱いやすい。

### 5.3 Michalsky (1988)：天体暦（Astronomical Almanac）の近似式

- Michalsky, J. J. "The Astronomical Almanac's algorithm for approximate solar position (1950–2050)." *Solar Energy*, 40(3), 227–235, 1988.
- 天体暦に掲載された近似式を、太陽エネルギー分野で使いやすい形にまとめた論文。
- 1950〜2050年の範囲で約0.01°の精度。太陽光発電・日射計測の分野で長く標準的に使われてきた。

### 5.4 Reda & Andreas (2004)：NREL Solar Position Algorithm（SPA）

- Reda, I. & Andreas, A. "Solar position algorithm for solar radiation applications." *Solar Energy*, 76(5), 577–589, 2004. DOI: 10.1016/j.solener.2003.12.003
- 訂正：*Solar Energy*, 81(6), 838, 2007（Corrigendum）。
- 技術報告版：NREL Report No. TP-560-34302（2003, 2008年改訂）。
- アメリカ国立再生可能エネルギー研究所（NREL）が開発。**−2000〜6000年の範囲で±0.0003°** という非常に高い精度を持つ。
- 惑星の位置理論 **VSOP87** を用いて地球の日心位置を求め、章動、光行差、大気差、地球時と世界時の差（ΔT, ΔUT1）、観測地点の標高・気温・気圧による補正まで含める。
- 太陽電池パネルなど任意の傾斜面への**入射角**も計算できる。
- 公式のC言語実装が公開されており、Python（`pvlib`、`sunposition`）、R（`solarPos`）、Rust など多数の移植がある。
- 本案件では過剰な精度だが、「厳密な根拠がほしい」「解説や論文で引用したい」場合の一次文献として最適。

### 5.5 Spencer (1971)：フーリエ級数による近似

- Spencer, J. W. "Fourier series representation of the position of the sun." *Search*, 2(5), 172, 1971.
- 赤緯と均時差を、年間の日数に対する数項のフーリエ級数（sin・cos の和）で近似する方法。
- 精度は赤緯で約0.035°程度と低めだが、式が非常に短い。**シェーダー内で直接計算**したい場合に便利。

### 5.6 Grena (2012)：2010〜2110年向けの高速アルゴリズム

- Grena, R. "Five new algorithms for the computation of sun position from 2010 to 2110." *Solar Energy*, 86(5), 1323–1337, 2012.
- 対象期間を100年に限定する代わりに、SPA より大幅に少ない計算量で高精度（最良の方式で約0.003°）を実現した5種類のアルゴリズムを提案。
- 組み込み機器など計算資源の限られた環境向け。

### 5.7 SunCalc（JavaScript ライブラリ）

- Vladimir Agafonkin 作。オランダの天文解説サイト「Astronomy Answers」の式をもとに実装。
- 太陽の方位角・高度に加え、日の出・日の入り、ゴールデンアワー、月の位置・月齢なども取得できる。
- three.js と組み合わせやすく、夕暮れや月の演出にも使える。

---

## 6. 照度センサー（設置できた場合）

### 6.1 安定性の評価

**センサー自体の安定性は高い。** 市販のデジタル照度センサーはドリフトが小さく、長時間の連続計測にも向いている。
不安定になりやすいのは**センサー以外の部分**（接続、電源、設置、PC側のソフト）。ここを設計でカバーすれば、展示用途として十分安定稼働できる。

### 6.2 センサーの選定

直射日光は約10万lxに達するため、測定上限の高いものを選ぶ。

| センサー | 測定範囲（目安） | 特徴 |
|---|---|---|
| BH1750（ローム） | 1〜65535 lx（設定変更で約10万lxまで拡張可） | 安価で情報が多い。窓越しの直射では飽和に注意 |
| TSL2591（ams） | 約0.0002〜88000 lx | ダイナミックレンジが広い。暗い曇天から明るい日まで対応 |
| VEML7700（Vishay） | 0〜約12万lx | 上限が高く、直射に近い環境でも使いやすい |

「何を測るか」も決める必要がある。

- **窓際で外の明るさを測る**：天気（晴れ・曇り）の変化を取りたい場合。
- **スクリーン付近で測る**：スクリーンに当たる光の強さに合わせて、見え方を自動補正したい場合。

### 6.3 構成（推奨）

```
[照度センサー] ─I2C(短く)─ [ESP32] ─USBケーブル(シリアル通信・給電)─ [再生PC]
```

- Wi-Fi は使わず、**USBシリアル通信**にする（ネットワーク不要で、接続も安定）。
- 電源はPCのUSBから取る。

### 6.4 不安定になりやすい点と対策

| 項目 | リスク | 対策 |
|---|---|---|
| I2C配線 | 配線が長いと通信エラーが増える | センサーとESP32は近く（数十cm以内）に置き、距離はUSBケーブル側で稼ぐ |
| USBケーブル | 長すぎると認識が不安定 | 3m程度まで。それ以上はアクティブ延長ケーブルやUSBハブを使う |
| ESP32の電源 | 電圧降下でリセット（ブラウンアウト） | 0.5A以上供給できる電源。ENピンにRC遅延回路（10kΩ + 1µF目安）を入れる |
| ファームウェア | センサーの応答停止、フリーズ | ウォッチドッグタイマーを有効化。I2Cエラー時はセンサーを再初期化 |
| データ送信 | PC側で途切れを検知できない | 一定間隔（5〜10Hz程度）で値を送り、ハートビートとして扱う |
| PC側の受信 | ケーブル抜けやCOMポート番号の変化 | 自動再接続処理。WindowsではUSBのセレクティブサスペンドを無効化 |
| 値の急変 | 雲の通過で値が大きく揺れる | 移動平均や指数平滑化で数秒〜数十秒かけて滑らかにする |
| 設置環境 | 人が触る・覆う、直射による熱、ほこり | 手の届かない位置に固定。小型ケースに入れる。直射の当たる窓際では温度に注意 |

### 6.5 フォールバック設計（最重要）

センサーはあくまで**上乗せ**として扱い、止まっても作品が成立するようにする。

1. 通常時：太陽位置の計算（ベース）＋センサー値による補正。
2. センサー値が一定時間（例：10秒）届かない場合：太陽高度から推定した明るさ（例：晴天時の明るさ ≈ sin(高度) に比例）に、数秒かけて**クロスフェード**で切り替える。
3. 復帰したら、同様にクロスフェードでセンサー値に戻す。

こうしておけば、センサーが止まっても映像が急に変わらず、鑑賞者には気づかれない。

---

## 7. 気象庁データ

- 気象庁「過去の気象データ・ダウンロード」から、事前にCSVを取得してローカルに同梱する。
- 使える項目：気温、日照時間、降水量、湿度、風向・風速、雲量など。
- CSVは Shift-JIS でヘッダーが複数行あり、品質情報や欠測の列が混ざるため、前処理してJSONなどに変換しておく。
- 政府標準利用規約に基づき、**出典の明記が必要**（例：「出典：気象庁ホームページ」）。作品のクレジットに入れる。
- 使い方の例：その日と同じ日付の過去データを使う、1年を数分に圧縮して見せる、など。

---

## 8. 懸念点のまとめ

### 表示・見え方

- 外光の強さは時間帯・季節・天気で大きく変わる。モニターでは判断できないので、実機・実環境での試写が必須。
- グラデーションのバンディング → ディザで対策。
- 2台のプロジェクターの色差が、ブレンド部をまたぐ色面で目立ちやすい → テストパターンで事前確認。
- ガラスの映り込みが映像と重なる → 最終的な投影面素材の決定が必要。

### 長時間運用

- 熱・電力対策。
- 無人運用：OSの通知・アップデート、スリープ、スクリーンセーバーを無効化。
- 停電後の自動復帰：BIOSの電源復帰設定、自動ログイン、アプリの自動起動と全画面化、クラッシュ時の自動再起動。

### ツール別

**three.js（Web）**

- ブラウザのフルスクリーンは1画面分 → 3840×1080の枠なしウィンドウか、Electronでアプリ化。
- `file://` ではCSVのfetchがCORSでブロックされる → ローカルサーバーか、データをJS/JSONに埋め込む。
- ブラウザの自動アップデートやスロットリング → Electronでバージョン固定。

**TouchDesigner**

- 無償の Non-Commercial 版は出力解像度が1280×1280まで → **Commercial ライセンスが必要**。
- オフライン環境でのライセンス認証 → 事前認証、またはCodeMeterドングル。
- Performモードでの自動起動、Window COPでの2画面スパン設定を事前検証。
- 現地で色や動きをリアルタイムに調整しやすく、本案件とは相性が良い。

---

## 9. 確認事項・次のアクション

- [ ] 映像の受け渡し仕様（3840×1080の1枚か、2分割か、重なり幅は何pxか）をブレンド担当と確定
- [ ] 実機プロジェクター・実環境での試写日程を確保
- [ ] 最終的な投影面の素材を決定
- [ ] 窓とスクリーンの向き（真北基準）を図面・地図で確認
- [ ] 設置場所の正確な緯度経度を確認（本メモの値は概算）
- [ ] 現地で数時間おきに光の当たり方を撮影し、遮蔽テーブルを作成
- [ ] 照度センサーの設置可否（窓際・スクリーン付近）を確認
- [ ] 上映時間・期間、再生PCの構成、音の有無を確認
- [ ] ツールを決定（TouchDesignerの場合はライセンスを手配）
- [ ] 気象庁データの取得と前処理
